export const COLORS = {
  primary: '#000000',
  secondary: '#ffffff',
};

export const FONTS = {};

// AsyncStorage에 사용자 정보 등록하는 키로 사용됨
export const authKey = 'userData';

export const baseURL = '';

export const API = {
  temp: '/temp',
};
